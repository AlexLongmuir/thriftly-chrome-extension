/** Turntable stops the backend can generate. 0° is always the original photo.
    Must stay in sync with VIEW_ANGLES in api/product-views.ts. */
export const TURNAROUND_ANGLES = [60, 120, 180, 240, 300] as const;
